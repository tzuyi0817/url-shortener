<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body">
      {{#if createUrl}}
      <h3 class="text">短網址創建成功！ 點擊連結您的網站：</h3>
      <h3><a href="{{createUrl}}">{{createUrl}}</a></h3>
      {{/if}}
    </div>

    <div class="card card-body">
      {{> messages}}
      <form method="POST" action='/'>
        <div class="form-group">
          <label for="url">請輸入要產生的網址:</label>
          <input type="url" class="form-control" id="url" name="url" placeholder="請在這裡輸入網址" value="{{url.originalUrl}}"
            required>
        </div>

        <button type="submit" class="btn btn-primary mt-5">shorten!</button>
      </form>

      {{#if existUrl}}
      <h3 class="text mt-5">已創建過的短網址！ 點擊連結您的網站：</h3>
      <h3><a href="{{existUrl}}">{{existUrl}}</a></h3>
      {{/if}}
    </div>
  </div>
</div>